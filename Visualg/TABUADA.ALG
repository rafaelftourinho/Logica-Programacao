Algoritmo "tabuada"

Var

   N, multiplicacao, i : inteiro

Inicio

   escreva("Deseja a tabuada para qual valor: ")
   leia(N)
   
   multiplicacao <- 1
   
   para i de 1 ate 10 faca
        multiplicacao <- N * i
        escreval(N, " x", i, " =", multiplicacao)
   fimpara


Fimalgoritmo