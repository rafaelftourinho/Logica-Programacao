Algoritmo "exemplo_aula"


Var

   delta, x1, x2 : real
   a, b, c, : inteiro

Inicio
   delta <- Exp(b,2.0) -4*a*c
   x1 <- (-b +RaizQ(delta)) / (2.0*a)
   x2 <- (-b - RaizQ(delta)) / (2.0*a)
   b <- 3
   a <- 1
   c <- 6
   
   escreval(x1)
   escreval(x2)
   


Fimalgoritmo